// GeneralizedHarmonic is a program that takes two integer command-line arguments n and r

//and uses a for loop

// to compute the nth generalize harmonic of order r



public class GeneralizedHarmonic {
    public static void main(int n, int r) {
        for (int i = 0; i <= n; i++) {
            h += 1 / (n ** r)
        }
    }
}